import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"What is RestPHP?","description":"","frontmatter":{},"headers":[],"relativePath":"guide/what-is-restphp.md","filePath":"guide/what-is-restphp.md","lastUpdated":1788589921000}');
const _sfc_main = { name: "guide/what-is-restphp.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="what-is-restphp" tabindex="-1">What is RestPHP? <a class="header-anchor" href="#what-is-restphp" aria-label="Permalink to &quot;What is RestPHP?&quot;">​</a></h1><p><strong>RestPHP</strong> is a modern, ultra-high-performance persistent application server and runtime for PHP, written in Rust.</p><p><strong>Important Positioning:</strong> RestPHP is a <strong>100% standalone sovereign runtime</strong>. It is not dependent on Laravel Octane. While it provides an optional first-class adapter for Laravel Octane to supercharge Laravel applications, RestPHP can run any plain PHP script or custom framework directly.</p><hr><h2 id="the-problem-with-traditional-php-hosting" tabindex="-1">The Problem with Traditional PHP Hosting <a class="header-anchor" href="#the-problem-with-traditional-php-hosting" aria-label="Permalink to &quot;The Problem with Traditional PHP Hosting&quot;">​</a></h2><p>For decades, the standard way to deploy PHP applications has been <strong>Nginx + PHP-FPM (FastCGI)</strong> or Apache with <code>mod_php</code>.</p><p>In this model:</p><ol><li>Every incoming HTTP request initiates a <strong>cold boot</strong>.</li><li>PHP loads the autoloader, thousands of framework files (Laravel/Symfony), compiles them to opcodes, boots service providers, and sets up routes.</li><li>Once the HTTP response is sent, the entire Zend VM instance and process memory are <strong>destroyed</strong>.</li><li>The next request repeats the entire process from scratch.</li></ol><p>Even with OPcache, <strong>70% to 80% of request time is spent re-bootstrapping the framework</strong>, severely limiting raw throughput to a few hundred requests per second.</p><hr><h2 id="enter-persistent-php-the-go-era-its-limits" tabindex="-1">Enter Persistent PHP: The Go Era &amp; Its Limits <a class="header-anchor" href="#enter-persistent-php-the-go-era-its-limits" aria-label="Permalink to &quot;Enter Persistent PHP: The Go Era &amp; Its Limits&quot;">​</a></h2><p>In recent years, tools like <strong>FrankenPHP</strong> and <strong>RoadRunner</strong> introduced persistent workers to PHP by writing servers in Go:</p><ul><li>The PHP application is loaded into RAM once.</li><li>Requests are handled persistently without reloading files.</li></ul><p>However, writing a persistent PHP runtime in Go comes with fundamental architectural penalties:</p><h3 id="_1-the-cgo-stack-switching-tax" tabindex="-1">1. The <code>cgo</code> Stack-Switching Tax <a class="header-anchor" href="#_1-the-cgo-stack-switching-tax" aria-label="Permalink to &quot;1. The \`cgo\` Stack-Switching Tax&quot;">​</a></h3><p>Go has its own runtime and lightweight segmented stacks (goroutines). Whenever Go calls C (the Zend Engine API), it must switch to a standard POSIX thread stack. This <code>cgo</code> transition costs ~60ns per invocation. Under 50,000 req/s, this context-switching overhead wastes substantial CPU cycles.</p><h3 id="_2-double-garbage-collection" tabindex="-1">2. Double Garbage Collection <a class="header-anchor" href="#_2-double-garbage-collection" aria-label="Permalink to &quot;2. Double Garbage Collection&quot;">​</a></h3><p>Go has an active Garbage Collector (Stop-The-World concurrent mark-sweep), and PHP has its own cyclic Garbage Collector. When running high traffic, the host Go runtime triggers GC pauses, causing jitter and degrading <strong>p99 tail latency</strong>.</p><hr><h2 id="the-restphp-solution-powered-by-rust" tabindex="-1">The RestPHP Solution: Powered by Rust <a class="header-anchor" href="#the-restphp-solution-powered-by-rust" aria-label="Permalink to &quot;The RestPHP Solution: Powered by Rust&quot;">​</a></h2><p>RestPHP was designed from the ground up to solve these problems by embedding the Zend Engine directly into <strong>Rust</strong>:</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">graph TD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    A[Incoming HTTP Request] --&gt; B[Tokio Async I/O Loop]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt; C[Lock-free Worker Queue]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    C --&gt; D[Dedicated Worker OS Thread]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    D --&gt; E[Zero-Cost C-ABI FFI]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    E --&gt; F[Persistent Zend VM]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    F --&gt; G[Custom SAPI Output Buffer]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    G --&gt; B</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    B --&gt; H[HTTP Client Response]</span></span></code></pre></div><h3 id="key-architectural-invariants" tabindex="-1">Key Architectural Invariants: <a class="header-anchor" href="#key-architectural-invariants" aria-label="Permalink to &quot;Key Architectural Invariants:&quot;">​</a></h3><ol><li><strong>Zero-Cost C-ABI</strong>: Rust and C share the exact same binary calling convention (<code>extern &quot;C&quot;</code>). Zero stack switches, zero glue overhead.</li><li><strong>Zero Host Garbage Collection</strong>: Rust uses compile-time ownership and deterministic RAII. There is no host GC runtime, eliminating latency spikes entirely.</li><li><strong>Rock-Solid Tail Latency (p99)</strong>: While other runtimes suffer latency jitter during memory pressure, RestPHP provides flat, deterministic response curves.</li><li><strong>100% PHP Extension Compatibility</strong>: Because RestPHP embeds the genuine Zend Engine C core, standard PHP extensions (PDO, Redis, cURL, GD, OPcache) work with zero modifications.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/what-is-restphp.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const whatIsRestphp = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  whatIsRestphp as default
};
